# Questionnaire Builder — Agent Instructions

React 19 + TypeScript SPA for building, sharing, and analysing questionnaires using **SurveyJS**. See [PLAN.md](Docs/Completed/PLAN.md) for full project history and [Docs/SurveyJS/](Docs/SurveyJS/README.md) for SurveyJS component docs.

---

## Commands

All commands can be run from the repo root (workspace delegates to `Frontend/`) or from `Frontend/` directly.

```bash
pnpm dev            # Vite dev server (http://localhost:5173)
pnpm build          # tsc -b && vite build
pnpm lint           # ESLint on Frontend/src/
pnpm lint:fix       # ESLint --fix
pnpm format         # Prettier on Frontend/src/
pnpm test           # Vitest (unit + integration)
pnpm coverage       # Vitest with coverage
pnpm test:e2e       # Playwright
```

Use `pnpm`, not `npm` or `yarn`.

> **Repo layout**: Frontend code lives in `Frontend/`. The root `package.json` and `pnpm-workspace.yaml` delegate all scripts to the `Frontend` package. To add dependencies, run `pnpm add <pkg>` from inside `Frontend/`.

---

## Architecture

| Concern | Tool | Key file |
|---------|------|----------|
| Routing | TanStack Router (file-based) | `Frontend/src/routes/` |
| Server state | TanStack React Query | `Frontend/src/hooks/` |
| Forms | TanStack React Form | `Frontend/src/lib/form.ts` |
| HTTP | Axios | `Frontend/src/lib/axios.ts` |
| Styling | Tailwind CSS v4 + Radix UI | `Frontend/src/index.css` |
| Types | Central interfaces | `Frontend/src/types/index.ts` |
| Form builder UI | React visual editor + Raw JSON editor (Monaco) | `Frontend/src/routes/questionnaire-types/$id/json.tsx` |
| Survey renderer | `survey-react-ui` | `Frontend/src/components/survey/SurveyRenderer.tsx` |
| Response analytics | `survey-analytics` | `Frontend/src/components/survey/SurveyDashboard.tsx` |
| PDF export | `survey-pdf` | used inside results page |
| Metrics utility | Pure functions | `Frontend/src/lib/metrics.ts` |
| Form builder conversion | Pure functions | `Frontend/src/lib/formBuilder.ts` |
| Type API | Axios | `Frontend/src/api/questionnaireTypes.ts` |
| Instance API | Axios | `Frontend/src/api/questionnaires.ts` |
| Type hooks | TanStack React Query | `Frontend/src/hooks/useQuestionnaireTypes.ts` |
| Instance hooks | TanStack React Query | `Frontend/src/hooks/useQuestionnaires.ts` |

---

## Feature Overview

The app has a **two-level model**: `QuestionnaireType` (the template/form definition) and `Questionnaire` (an instance/deployment with responses).

Three user flows:

```
1. Builder  →  /questionnaire-types/:id/json
              Raw JSON editor → saves surveyJson to QuestionnaireType via PATCH

2. Respondent  →  /take/:shareToken     (shareable public URL, no auth)
              Survey renderer reads surveyJson from QuestionnaireType
              → onComplete → PATCH /questionnaires/by-token/:token/submit/

3. Owner  →  /questionnaires/:id/results
              Fetches Questionnaire instance → VisualizationPanel renders charts
              Optional: export to PDF via survey-pdf
```

**Shareable URL pattern**: `window.location.origin + /take/${questionnaire.shareToken}`
Copy-to-clipboard button lives on the questionnaire types page (`/questionnaire-types/share`).

---

## Route Structure

```
Frontend/src/routes/
  __root.tsx                         # layout (nav bar)
  index.tsx                          # redirect → /questionnaire-types
  questionnaire-types/
    index.tsx                        # list of QuestionnaireTypes with Edit / Deploy buttons
    new.tsx                          # create new QuestionnaireType → redirects to /json
    share.tsx                        # /questionnaire-types/share — shareable URLs for deployed instances
    $id/
      json.tsx                       # split-view: JSON editor (left) + live SurveyRenderer preview (right)
      index.tsx                      # redirect → json
  questionnaires/
    index.tsx                        # list of deployed Questionnaire instances (Submitted/Pending badges)
    new.tsx                          # deploy a new instance from a QuestionnaireType
    $id/
      results.tsx                    # SurveyJS Dashboard + PDF export + Raw Responses table
      index.tsx                      # redirect → results
  take/
    $id.tsx                          # Public respondent view — :id is the shareToken (no auth)
```

Never edit `Frontend/src/routeTree.gen.ts` — it is auto-generated by the Vite plugin.

---

## SurveyJS Package Roles

| Package | npm install | Role | License |
|---------|-------------|------|---------|
| `survey-core` | peer dep | JSON model + logic engine | MIT |
| `survey-react-ui` | `pnpm add survey-react-ui` | Renders surveys to respondents | MIT |
| `survey-analytics` | `pnpm add survey-analytics` | Response charts dashboard | Commercial |
| `survey-pdf` | `pnpm add survey-pdf` | Client-side PDF export | Commercial |

See [Docs/SurveyJS/](Docs/SurveyJS/README.md) for full per-package docs.

---

## SurveyJS Integration Conventions

> See [`.github/instructions/surveyjs.instructions.md`](.github/instructions/surveyjs.instructions.md) for detailed patterns.

- **`QuestionnaireType.surveyJson`** (type `object`) stores the raw SurveyJS JSON schema. The old `questions: Question[]` array is superseded by this field.
- **JSON editor** saves via `PATCH /questionnaire-types/:id` with `{ surveyJson: <parsed JSON> }`.
- **Survey Renderer** submits via `survey.onComplete.add` → `PATCH /questionnaires/by-token/:token/submit/` (sets `answers` and `submittedAt`).
- **SurveyJS components are not SSR-safe** — this is a Vite SPA so no issue, but do not attempt server rendering.
- Always import SurveyJS CSS explicitly (see instructions file).
- `survey-creator-react` and `survey-creator-core` have been **removed** — do not re-add them.

---

## Key Conventions

### Routing
- Routes live in `Frontend/src/routes/` — the Vite plugin auto-generates `Frontend/src/routeTree.gen.ts`. **Never edit that file.**
- Root layout: `Frontend/src/routes/__root.tsx`. New routes follow the same export pattern as `Frontend/src/routes/index.tsx`.

### State / Queries
- Follow the query-key factory pattern in [`Frontend/src/hooks/useQuestionnaires.ts`](Frontend/src/hooks/useQuestionnaires.ts).
- Mutations must call `queryClient.invalidateQueries()` on success to refresh lists.

### Forms
- Simple forms use controlled React state or `@tanstack/react-form`.
- Validators receive `{ value }` (not the raw value) — TanStack Form v1 API.

### API Layer
- Add typed functions to the relevant file under `Frontend/src/api/`. Follow the Omit pattern for payloads (`Omit<T, 'id' | 'createdAt' | 'updatedAt'>`).
- Auth token is read from `localStorage` by the Axios interceptor — no manual headers needed.
- Set `VITE_API_BASE_URL` in `Frontend/.env` to point at the backend; defaults to `/api`.
- **QuestionnaireType API**: `Frontend/src/api/questionnaireTypes.ts` — CRUD against `/questionnaire-types/`.
- **Questionnaire instance API**: `Frontend/src/api/questionnaires.ts` — CRUD against `/questionnaires/`; `getQuestionnaireByToken(token)` hits `/questionnaires/by-token/:token/`; `submitAnswers(token, answers)` PATCHes `/questionnaires/by-token/:token/submit/`.
- **QuestionnaireType hooks**: `Frontend/src/hooks/useQuestionnaireTypes.ts` — `questionnaireTypeKeys`, `useQuestionnaireTypes`, `useQuestionnaireType`, `useCreateQuestionnaireType`, `useDeleteQuestionnaireType`.
- **Questionnaire instance hooks**: `Frontend/src/hooks/useQuestionnaires.ts` — `questionnaireKeys`, `useQuestionnaires`, `useQuestionnaire`, `useQuestionnaireByToken`, `useCreateQuestionnaire`, `useDeleteQuestionnaire`, `useSubmitAnswers`.
- `api/responses.ts` and `hooks/useResponses.ts` are deprecated stubs — do not use.
- The `/take/:shareToken` route calls `useQuestionnaireByToken` (public, no auth); answers submitted without auth via `useSubmitAnswers`.

### UI Components
- Reusable primitives live in `Frontend/src/components/ui/`. Use the `cn()` helper from `Frontend/src/lib/utils.ts` for className merging.
- Use CVA variants + `asChild` (Radix Slot) pattern — see [`Button.tsx`](Frontend/src/components/ui/Button.tsx).
- Form builder components live in `Frontend/src/components/formBuilder/`: `QuestionList`, `QuestionEditor`, `SurveyTitleEditor`, `AdvancedQuestionPlaceholder`. Barrel export via `index.ts`.
- Tailwind v4: theme values are CSS variables (e.g. `var(--color-primary)`) defined in `Frontend/src/index.css`.

### TypeScript
- Strict mode + `noUnusedLocals` + `noUnusedParameters` — unused variables cause **build failures**.
- `QuestionnaireType.surveyJson` is `object` (raw SurveyJS JSON). The old `questions: Question[]` array is **not** used for SurveyJS-powered forms.
- `Questionnaire` (instance) has `answers: Record<string, unknown>`, `metrics?: Record<string, unknown>` (pre-computed calculatedValues stored at submit time; empty `{}` if not yet submitted or pre-PLAN-15), `surveyJsonSnapshot: object` (snapshot of the type's `surveyJson` taken at submit time; empty `{}` if not yet submitted), and `submittedAt: string | null` (null = not yet submitted).
- Import alias `@/` maps to `Frontend/src/`.

---

## Testing

- **Unit/integration**: Vitest + Testing Library. Files in `Frontend/src/test/`. Globals (`describe`, `it`, `expect`, `vi`) are available without importing.
- **Setup**: [`Frontend/src/test/setup.ts`](Frontend/src/test/setup.ts) imports `@testing-library/jest-dom`.
- Each test file should wrap components with `QueryClientProvider` using a fresh `QueryClient({ defaultOptions: { queries: { retry: false } } })` to avoid cache pollution and timeout flakiness.
- Mock API calls with `vi.mock('../api/<file>')` at the top of the test. See [`JsonEditorPage.test.tsx`](Frontend/src/test/JsonEditorPage.test.tsx) for the full pattern.
- To mock a class used with `new` (e.g. `survey-core` `Model`), use `vi.hoisted()` + a `class` in the factory — arrow functions in `mockImplementation` are not constructors. See [`ResultsPage.test.tsx`](Frontend/src/test/ResultsPage.test.tsx) for the pattern.
- Use exact string matching (`findByText('Pending')`) not regex (`/Pending/i`) when the regex could match multiple elements with similar text (e.g. badge vs. title).
- **Current test count**: 171 frontend tests (19 files), 87 backend tests.
- **E2E**: Playwright tests in `Frontend/e2e/`. Base URL: `http://localhost:5173`.

### Frontend test files

| File | Covers |
|------|--------|
| `formBuilder.test.ts` | `parseSurveyJson`, `buildSurveyJson`, `generateUniqueName` utilities |
| `QuestionList.test.tsx` | QuestionList component: render, add, delete, reorder |
| `QuestionEditor.test.tsx` | QuestionEditor: type toggle shows/hides fields, onChange, duplicate name |
| `metrics.test.ts` | `evaluateMetrics`, `metricsFromStored`, `formatLabel` utilities |
| `CopyButton.test.tsx` | CopyButton UI component |
| `JsonEditorPage.test.tsx` | `/questionnaire-types/:id/json` split-view editor |
| `NewQuestionnairePage.test.tsx` | `/questionnaires/new` deploy form |
| `QuestionnairesPage.test.tsx` | `/questionnaires` list page |
| `RawResponsesTable.test.tsx` | RawResponsesTable component |
| `ResultsPage.test.tsx` | `/questionnaires/:id/results` metrics page |
| `ShareLinksPage.test.tsx` | `/questionnaire-types/share` shareable URLs |
| `TakePage.test.tsx` | `/take/:shareToken` respondent view |
| `api.questionnaires.test.ts` | `api/questionnaires.ts` functions |
| `api.responses.test.ts` | deprecated responses API stub |
| `home.test.tsx` | root redirect |
| `useQuestionnaires.test.tsx` | `useQuestionnaireTypes` hooks (note: file name is a misnomer) |
| `useQuestionnairesHooks.test.tsx` | `useQuestionnaires` instance hooks incl. `useUpdateQuestionnaire`, `useSubmitAnswers` |
| `useResponses.test.tsx` | deprecated responses hooks |
| `utils.test.ts` | `cn()`, `buildShareUrl()` utilities |

---

## Agent Customization — Auto-Create Policy

**Always create or update customization files as part of the task. Never only suggest them.**

When any of the triggers below apply, create the file immediately alongside the code changes.

### When to create / update each type

| Trigger | File type | Location |
|---------|-----------|----------|
| A new library, framework, or package is integrated | `.instructions.md` | `.github/instructions/<topic>.instructions.md` |
| A repeatable multi-step workflow is introduced (new resource, new route, new component pattern) | `.prompt.md` | `.github/prompts/<task>.prompt.md` |
| A convention is discovered that differs from common practice | Add a rule to the relevant `.instructions.md` | — |
| An existing `applyTo` glob doesn't cover new file paths being created | Update `applyTo` in the relevant `.instructions.md` | — |
| A set of steps would benefit from agent isolation or tool restriction | `.agent.md` | `.github/agents/<name>.agent.md` |
| `AGENTS.md` is missing context an agent would need to be productive | Update `AGENTS.md` | root |

### Rules

- **Do it, don't mention it.** Create or edit the file in the same response as the code. Do not end a response with "you could create a prompt for this".
- **Link, don't duplicate.** Reference existing docs with Markdown links rather than copying their content into the new file.
- **Minimal and actionable.** Every line in a customization file must guide concrete behaviour. Remove vague or obvious content.
- **Keep `applyTo` tight.** Use specific globs (e.g. `src/hooks/**`, `src/routes/take/**`). Avoid `**` unless the instruction genuinely applies everywhere.
- **Prompts include a verification step.** Every `.prompt.md` that scaffolds code must end with a step that runs `pnpm build` (or `pnpm test`) and fixes any errors.

### Existing customization files

| File | Covers |
|------|--------|
| [`AGENTS.md`](AGENTS.md) | Project-wide conventions, architecture, commands |
| [`.github/instructions/surveyjs.instructions.md`](.github/instructions/surveyjs.instructions.md) | SurveyJS component patterns (Renderer, Dashboard, PDF) |
| [`.github/instructions/survey-json.instructions.md`](.github/instructions/survey-json.instructions.md) | Survey definition JSON, response data shapes, calculated values, conditional logic, Django field mapping |
| [`.github/instructions/ui-primitives.instructions.md`](.github/instructions/ui-primitives.instructions.md) | CVA variants, Radix `asChild`, `cn()`, Tailwind v4 theming |
| [`.github/prompts/add-route.prompt.md`](.github/prompts/add-route.prompt.md) | Scaffold a single TanStack Router route + test |
| [`.github/prompts/add-query-hook.prompt.md`](.github/prompts/add-query-hook.prompt.md) | Scaffold a React Query hook + Axios API file |
| [`.github/prompts/implement-questionnaire-routes.prompt.md`](.github/prompts/implement-questionnaire-routes.prompt.md) | Full questionnaire build→share→respond→results workflow |
