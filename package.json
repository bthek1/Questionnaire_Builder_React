{
  "name": "questionnaire-builder-root",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter questionnaire-builder dev",
    "build": "pnpm --filter questionnaire-builder build",
    "preview": "pnpm --filter questionnaire-builder preview",
    "lint": "pnpm --filter questionnaire-builder lint",
    "lint:fix": "pnpm --filter questionnaire-builder lint:fix",
    "format": "pnpm --filter questionnaire-builder format",
    "test": "pnpm --filter questionnaire-builder test",
    "test:watch": "pnpm --filter questionnaire-builder test:watch",
    "coverage": "pnpm --filter questionnaire-builder coverage",
    "test:e2e": "pnpm --filter questionnaire-builder test:e2e"
  }
}
